<nav class="flex flex-wrap items-center justify-between gap-4 border-b border-primary/10 pb-4">
  <div class="flex items-center gap-4">
    <span class="text-lg font-semibold text-primary">Admin</span>
    <%= link_to "Catalog", admin_catalog_products_path, class: "text-sm font-semibold text-primary/80 hover:text-primary" %>
    <%= link_to "Shipping", admin_shipping_rates_path, class: "text-sm font-semibold text-primary/80 hover:text-primary" %>
  </div>
  <%= link_to "View site", product_path, class: "text-sm font-semibold text-primary/70 hover:text-primary" %>
</nav>
