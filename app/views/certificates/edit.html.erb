<% content_for :title do %>
  Edit Certificate
<% end %>

<%= render "shared/modal_wrapper", dialog_id: "certificate-form", title: "Edit Certificate", close_path: certificate_path(@certificate) do |close_action| %>
  <%= render "form", certificate: @certificate, close_path: certificate_path(@certificate) %>
<% end %>
