<div class="<%= field_row_classes %>">
  <%= form.label attribute, label_text, class: form_label_classes %>
  <%= field_html %>

  <% if local_assigns[:hint].present? %>
    <p class="text-sm text-content-muted"><%= hint %></p>
  <% end %>
</div>
